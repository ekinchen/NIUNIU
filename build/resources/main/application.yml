app:
  instance: ${random.uuid}
  
spring:
  application:
    name: "NIUNIU"
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://10.132.1.8:3306/petstore?characterEncoding=UTF-8&zeroDateTimeBehavior=convertToNull
    username: petstore
    password: uYllMdKzvTmS
    tomcat.maxActive: 60
    tomcat.initSQL: "select 1 from dual"
  jpa:
    hibernate:
      use-new-id-generator-mappings: false
      ddl-auto: create-drop
    show-sql: true


    

